inlude<stdio.h>
int main()
{
    int t, n, r, c, sr, sc;/// declaratiion of variables
    scanf("%d",&t);//test=t
    while(t!=0){
        scanf("%d",&n);//n= number of instructions
        scanf("%d",&r);//r=numberof rows
        scanf("%d",&c);//c=number if columns
        scanf("%d",&sr);//sr=starting point in row
        scanf("%d",&sc);//sc= starting point in column
        //instructions
        for(int i=0;i<n;i++)
        {
            
        }
    }
}